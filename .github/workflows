name: Save Secret to File
on: [push]
jobs:
  save_secret:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Save secret to file
        run: echo ${{ secrets.DB }} > secret.txt
